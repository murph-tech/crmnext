# 📖 คู่มือการ Deploy บน Ubuntu (ฉบับละเอียด - SQLite Version)

คู่มือนี้จะสอนการนำ CRM Next ขึ้น Server จริงแบบทีละขั้นตอน สำหรับผู้เริ่มต้น
**ไม่ต้องใช้ Docker** และ **ไม่ต้องลง Database Server** ให้ยุ่งยาก

---

## สารบัญ
1. [สิ่งที่ต้องเตรียม](#1-สิ่งที่ต้องเตรียม)
2. [ขั้นตอนที่ 1: เตรียม Server](#ขั้นตอนที่-1-เตรียม-server)
3. [ขั้นตอนที่ 2: อัปโหลดไฟล์ขึ้น Server](#ขั้นตอนที่-2-อัปโหลดไฟล์ขึ้น-server)
4. [ขั้นตอนที่ 3: ติดตั้งและตั้งค่า](#ขั้นตอนที่-3-ติดตั้งและตั้งค่า)
5. [ขั้นตอนที่ 4: ตั้งค่า Domain และ SSL (ให้เว็บเป็น HTTPS)](#ขั้นตอนที่-4-ตั้งค่า-domain-และ-ssl)

---

## 1. สิ่งที่ต้องเตรียม
1. **Cloud Server (VPS)**
   - แนะนำ Ubuntu 22.04 LTS หรือ 24.04 LTS
   - สเปคขั้นต่ำ: RAM 2GB, CPU 1 Core (แนะนำ RAM 4GB ขึ้นไปจะลื่นกว่า)
   - ผู้ให้บริการแนะนำ: DigitalOcean, Vultr, Linode, AWS Lightsail
2. **Domain Name**
   - จดโดเมนที่ต้องการ (เช่น `mycrm.com`)
   - ชี้ A Record มาที่ IP Address ของ Server คุณ

---

## ขั้นตอนที่ 1: เตรียม Server

1. **Login เข้า Server ผ่าน Terminal (Mac/Linux) หรือ PuTTY (Windows)**
   ```bash
   ssh root@your-server-ip
   # ใส่ password ที่ได้จากผู้ให้บริการ
   ```

2. **รันคำสั่งติดตั้งโปรแกรมพื้นฐาน** (Copy ไปวางได้เลย)
   ```bash
   # อัปเดตเครื่อง
   apt update && apt upgrade -y

   # ติดตั้ง Node.js 20
   curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
   apt install -y nodejs build-essential

   # ติดตั้ง Nginx (เว็บเซิร์ฟเวอร์)
   apt install -y nginx

   # ติดตั้ง Git (เผื่อใช้)
   apt install -y git

   # ติดตั้ง PM2 (ตัวช่วยรันโปรแกรมให้ทำงานตลอดเวลา)
   npm install -g pm2
   ```

---

## ขั้นตอนที่ 2: อัปโหลดไฟล์ขึ้น Server

มี 2 วิธีหลัก (เลือกวิธีใดวิธีหนึ่ง):

### วิธี A: ใช้ Git (แนะนำ ถ้าเก็บโค้ดไว้บน GitHub/GitLab)
```bash
cd /var/www
git clone https://github.com/your-username/your-repo.git crm-next
cd crm-next
```

### วิธี B: อัปโหลดไฟล์จากคอมเรา (ถ้าไม่มี Git)
1. **บนเครื่อง Mac ของคุณ:** เปิด Terminal ในโฟลเดอร์โปรเจค
2. พิมพ์คำสั่งเพื่ออัปโหลด (เปลี่ยน IP เป็นของจริง)
   ```bash
   # ลบ folder node_modules ก่อนส่ง (เพื่อความเร็ว)
   scp -r . root@your-server-ip:/var/www/crm-next
   ```
   *หรือใช้โปรแกรม FileZilla ลากไฟล์ทั้งหมดไปใส่ที่ `/var/www/crm-next`* (ยกเว้น `node_modules`, `.git`, `.next`)

---

## ขั้นตอนที่ 3: ติดตั้งและตั้งค่า

กลับมาที่ Terminal ของ Server (SSH):

1. **เข้าไปที่โฟลเดอร์โปรเจค**
   ```bash
   cd /var/www/crm-next
   ```

2. **ติดตั้ง Libraries**
   ```bash
   # ติดตั้งของ Frontend
   npm ci

   # ติดตั้งของ Backend
   cd backend
   npm ci
   cd ..
   ```

3. **ตั้งค่า Backend (.env)**
   ```bash
   nano backend/.env
   ```
   ใส่ข้อมูลตามนี้ (แก้ไข URL เป็นของจริง):
   ```env
   PORT=4000
   NODE_ENV=production
   # ใช้ SQLite (ไฟล์ database จะชื่อ crm_prod.db)
   DATABASE_URL="file:./crm_prod.db"
   
   # ตั้งรหัสลับอะไรก็ได้ ยาวๆ
   JWT_SECRET=super_secret_password_change_me
   
   # URL ของเว็บเรา
   FRONTEND_URL=https://mycrm.com
   ```
   กด `Ctrl+X` -> `Y` -> `Enter` เพื่อบันทึก

4. **ตั้งค่า Frontend (.env.local)**
   ```bash
   nano .env.local
   ```
   ใส่ข้อมูล:
   ```env
   # ชี้ไปที่ API ของเรา
   NEXT_PUBLIC_API_URL=https://api.mycrm.com
   ```
   กด `Ctrl+X` -> `Y` -> `Enter` เพื่อบันทึก

5. **สร้าง Database และ Build โปรแกรม**
   ```bash
   # 1. สร้าง Database
   cd backend
   npx prisma generate
   npx prisma db push   # คำสั่งนี้จะสร้างไฟล์ crm_prod.db จริงๆ

   # 2. Build Backend
   npm run build

   # 3. Build Frontend
   cd ..
   npm run build
   ```

6. **เริ่มรันโปรแกรมด้วย PM2**
   ```bash
   pm2 start ecosystem.config.json
   
   # ตั้งให้รันอัตโนมัติเมื่อเปิดเครื่องใหม่
   pm2 save
   pm2 startup
   ```

---

## ขั้นตอนที่ 4: ตั้งค่า Domain และ SSL

ทำให้เข้าเว็บผ่าน `https://mycrm.com` ได้

1. **สร้างไฟล์ตั้งค่า Nginx**
   ```bash
   nano /etc/nginx/sites-available/crm-next
   ```

2. **ใส่ Config นี้ลงไป** (เปลี่ยน `mycrm.com` เป็นโดเมนของคุณ)
   ```nginx
   # ส่วนของ Frontend
   server {
       server_name mycrm.com;  # <-- แก้ตรงนี้

       location / {
           proxy_pass http://localhost:3000;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection 'upgrade';
           proxy_set_header Host $host;
           proxy_cache_bypass $http_upgrade;
       }
   }

   # ส่วนของ Backend API
   server {
       server_name api.mycrm.com;  # <-- แก้ตรงนี้

       location / {
           proxy_pass http://localhost:4000;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection 'upgrade';
           proxy_set_header Host $host;
           proxy_cache_bypass $http_upgrade;
       }
   }
   ```
   กด `Ctrl+X` -> `Y` -> `Enter` เพื่อบันทึก

3. **เปิดใช้งาน Config**
   ```bash
   ln -s /etc/nginx/sites-available/crm-next /etc/nginx/sites-enabled/
   rm /etc/nginx/sites-enabled/default  # ลบไฟล์ default เดิมทิ้ง
   nginx -t             # ตรวจสอบว่า config ถูกต้องไหม (ต้องขึ้น OK)
   systemctl restart nginx
   ```

4. **ติดตั้ง SSL (HTTPS) ฟรี ด้วย Certbot**
   ```bash
   # ติดตั้ง Certbot
   apt install -y certbot python3-certbot-nginx

   # ขอใบรับรอง (ทำตามขั้นตอนที่ขึ้นบนหน้าจอ)
   certbot --nginx -d mycrm.com -d api.mycrm.com
   ```

---

## 🎉 เสร็จสิ้น!
ตอนนี้คุณสามารถเข้าใช้งาน CRM ได้ที่ `https://mycrm.com`

### คำสั่งที่มีประโยชน์
- **ดูสถานะ Server:** `pm2 status`
- **ดู Logs (ถ้ามีปัญหา):** `pm2 logs`
- **Restart ระบบ:** `pm2 restart all`
